name: Publish Python Package

on:
  release:
    types: [created]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          set -ex
          while [ ! -f metadata.txt ]
          do 
            sleep 1
            gh release download
          end